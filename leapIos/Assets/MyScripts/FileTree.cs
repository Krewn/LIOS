using UnityEngine;
using System.Collections;

public class FileTree : MonoBehaviour {
	//This script will produce a file tree from the icon it is atached to.

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
